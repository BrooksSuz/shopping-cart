const ShoppingCart = () => {
  return (
    <div className="shopping-cart">
      <h1>This is inside the Shopping Cart</h1>
      <button className="checkout">Checkout</button>
    </div>
  );
};

export default ShoppingCart;
